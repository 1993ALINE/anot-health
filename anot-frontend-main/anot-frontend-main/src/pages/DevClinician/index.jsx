import Clinician from '../Clinician/index'

/**
 * Local dev entry for the clinician workspace at /dev-clinician.
 * Renders the same UI as /clinician (sign in as a clinician user first).
 */
export default function DevClinicianPage() {
  return <Clinician />
}
