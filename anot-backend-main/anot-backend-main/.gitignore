node_modules

# Environment / secrets — never commit any .env variant (.env, .env.backup, .env.production, ...)
.env*
!.env.example

uploads/
src/uploads/
*.log

# Build artifacts
*.zip
*.tar.gz
coverage/
test-fixtures/deepgram/*.wav
test-fixtures/deepgram/*.mp3

